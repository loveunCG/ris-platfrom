var PortletDraggable = function() {
    var e = function() {
            jQuery().sortable && $("#sortable_portlets1").sortable({
                connectWith: ".portlet",
                items: ".portlet",
                opacity: .8,
                handle: ".portlet-title",
                coneHelperSize: !0,
                placeholder: "portlet-sortable-placeholder",
                forcePlaceholderSize: !0,
                tolerance: "pointer",
                helper: "clone",
                tolerance: "pointer",
                forcePlaceholderSize: !0,
                helper: "clone",
                cancel: ".portlet-sortable-empty, .portlet-fullscreen",
                revert: 250,
                update: function(e, t) {
                    t.item.prev().hasClass("portlet-sortable-empty") && t.item.prev().before(t.item)
                }
            })
        },
        r = function() {
            jQuery().sortable && $("#sortable_portlets").sortable({
                connectWith: ".portlet",
                items: ".portlet",
                opacity: .8,
                handle: ".portlet-title",
                coneHelperSize: !0,
                placeholder: "portlet-sortable-placeholder",
                forcePlaceholderSize: !0,
                tolerance: "pointer",
                helper: "clone",
                tolerance: "pointer",
                forcePlaceholderSize: !0,
                helper: "clone",
                cancel: ".portlet-sortable-empty, .portlet-fullscreen",
                revert: 250,
                update: function(e, t) {
                    t.item.prev().hasClass("portlet-sortable-empty") && t.item.prev().before(t.item)
                }
            })
        }
    return {
        init: function() {
            e(), r()

        }
    }

}();
jQuery(document).ready(function() {
    PortletDraggable.init()
});